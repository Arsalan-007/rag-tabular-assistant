# The prebuilt Chroma vector store ships as an immutable tarball (Git LFS).
# Runtime code extracts it to data/chroma/ (gitignored) on first use.
# Rebuild + repack with `make ingest`.
data/chroma.tar.gz filter=lfs diff=lfs merge=lfs -text
